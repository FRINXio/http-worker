apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-worker-poller.fullname" . }}-test-connection"
  labels:
    {{- include "http-worker-poller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-worker-poller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
